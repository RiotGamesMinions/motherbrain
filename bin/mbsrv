#!/usr/bin/env ruby
$:.push File.expand_path("../../lib", __FILE__)
require 'motherbrain'

config = MB::Config.from_file(MB::Config.default_path)
config.rest_gateway.enable = true

MB::Application.run(config)
